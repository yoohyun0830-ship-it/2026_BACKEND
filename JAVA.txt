    [ 자바 실행순서 ]
        1. src 폴더내 .java 확장자 파일에서 java 코드 작성
        2. 빌드(build) : run java 실행
        3. [컴파일] bin 폴더내 .class 확장자 파일 (자동)생성

    [ 주석 ]
        1. // 한줄씩
        2. /**/ 여러줄 주석

    [용어] 

        1. package : 해당 클래스가 위치한 폴더명
        2. class : 자바에서 컴파일 하는 최소의 단위 *클래스{ } 안에서 코드 작성*
        3. main 함수 : 자바에서 * 코드 실행 *하는 흐름 = 스레드 제공

    [자료] : 데이터

        1. 리터럴 : 상수
        2. 자료타입(형)
            (1) boolean   1byte   true/false
            (2) char      2byte   '' 감싼 문자 1개 저장
            (3) byte      1byte   -128 ~ +127
            (4) short     2byte   -+ 3만 정도
            (5) int       4byte   -+ 21억 정도      *정수 리터럴 기본타입*
            (6) long      8byte   -+ 21억 이상  , L/l
            (7) float     4byte   소수점 8자리 표현 , F/f
            (8) double    8byte   소수점 17자리 표현    *실수 리터럴 기본타입*
            (9) String    N*2byte "" 감싼 문자N개 저장 ,  문자열객체(배열)

    [출력함수]

        1. System.out.print( ) : 출력
        2. System.out.println( ) : 출력 + 줄바꿈
        3. System.out.printf( ) : ( "형식문자" , 자료 ) : 형식문자
        (*) 이스케이프/제어 문자 : \n \t \' \" \\
        (*) 형식문자 : %s문자열 %d정수 %c문자 %f실수 , %자릿수s %-자릿수s %0자릿수s %전체자릿수.소수점자릿수f

    [입력함수]: 다양한 입력방식 제공

        1. 입력갹체 생성( 클래스 마다 1번씩)
            scanner scan = new Scanner( System.in );
        2. 입력객체 통한 입력함수 (nextXXX(), 타입주의 , 여러개 next()엔터기준으로 구분)
            타입 변수명 = scan.nextXXX();

    [ 타변변환 ]

        * 다형성 : (하나의 자료/값) 다양한 형태(타입) 갖는 성질
        (1) 자동타입변환 : byte -> short -> int -> long -> float -> double , * 연산 결과 *
        (2) 강제타입변환 : double -> float -> long -> int -> short -> byte , (변환할타입명)자료

    [ 연산자 ]

        1. 산술연산자: + - * / %
        2. 연결연산자: +
        3. 비교연산자: > < >= <= == !=
        4. 논리연산자: && || !
        5. 증감연산자: ++변수명 , 변수명++ , --변수명 , 변수명--
        6. 대입연산자: =  , 복합대입연산자: +=  -=  *=  /=  %=
        7. 삼항연산자: 조건 ? 참 : 거짓  , 중첩:  조건1 ? 참1 : 조건2 ? 참2 : 거짓  -> 참/거짓 
        * 문자열비교함수: 문자열A.equals( 문자열B )

    [ 조건문 ] : 조건에 따른 코드 흐름 제어

        1. if( 조건 ) 실행문;
        2. if( 조건 ){ 실행문; 실행문; }
        3. if( 조건 ){ } else{ }
        4. if( 조건 ){ } else if( 조건 ){ } else if( 조건 ){ } else{ }
        5. switch( 자료 ){
            case 값1 : 실행문; break;
            case 값2 : 실행문; break;
            case 값3 : 실행문; break;
            default : 실행문;
        }

    [ 반복문 ] : 조건에 따른 코드 반복 제어

        1. for( 초기식 ; 조건식 ; 증감식 ){ }
        * break; continue;

        2. while( 조건식 ){ 증감식; }

        3. for( 타입 변수명 : 배열 ){ }
        * 배열타입이 int 이면 왼쪽 변수 타입도 int 맞춘다.

    [ 배열 ] *동일한타입*의 여러 자료들을 *하나의* 배열타입

        1. 선언 : 
            (1) 타입[ ] 변수명 = new 타입[개수] 
            (2) 타입[ ] 변수명 = { 값1, 값2, 값3 }
        2. 특징 : 
            (1) 동일한 타입끼리 
            (2) 정적(고정)길이 
            (3) 초기값이 없으면 기본값 할당( 정수:0 , 실수:0.0 , 논리: false , 객체: null )
        3. 호출 :
            (1) 변수명 , 배열의 주소값 반환      : 배열의 주소값 반환 , I@372f7a8d 출력
            (2) Arrays.toString( 배열변수 )     : 배열의 요소들을 문자열로 반환 , [ ]출력
            (3) 배열변수[인덱스]                : 배열내 특정한 인덱스의 요소값 반환
            * 인덱스란? 배열내 요소들이 저장된 순서번호(0~)
        4. 요소값수정 : 배열변수[인덱스] = 새로운 값
            * 요소 삭제 없음 ,  요소 추가 없음

    [객체] 객체지향 프로그래밍

        1. 정의 : 상태(상태변수/필드/속성) 와 행위(메소드/함수) (추상) 하는 것
        2. 클래스 : 상태와 행위 코드로 작성(설계도)
            -> 용도 : (1) 설계 (2) 실행 (main 함수 갖는)
        3. 인스턴스 : 특정 클래스로 생성(new)한 객체(실체)
        4. 사용법
            (1) 클래스 생성
                - class 클래스명{ 멤버변수; 생성자; 메소드; }
                - 멤버변수 : 
                            타입 변수명;         * 만일 초기값이 없으면 기본값*(정수:0, 실수:0.0, 논리:false, 객체:null)
                            타입 변수명 = 초기값;
            (2) 인스턴스 생성
                - new 생성자명( );               * new 한개당 인스턴스 1개 생성(즉 메모리 주소값 생성) *
                - 타입 변수명 = new 생성자명( );
            (3) 인스턴스 참조
                - 객체변수명.멤버변수명;          * .(접근/도트)연산자: 변수가 갖는 객체의 주소로 이동(포인터) *
                - null : 변수가 null 이면 이동할 주소(객체)가 없다.
        (*) 객체는 변수가 참조하지 않으면 자동 사라진다. ( GC )

        선언되는 변수 위치에 따라 이름이 다르다.
        지역변수 : if / for / func(함수) {} 안에서 선언된 변수

        멤버변수 : 클래스 / 설계도 { } 안에서 선언되고 객체(인스턴스) 내 변수
                this : 해당 생성자/메소드 호출한 객체(인스턴스) 가리킴
                
        매개변수 : 생성자 / 메소드 호출하면서 인수값들을 저장하는 변수

    [ 생성자 ] 

        (1) 인스턴스(객체) 생성할 때 초기화 담당
            new 생성자명( )
        (2) 사용법
            1. 클래스 내부 선언
            2. 클래스명 동일
            3. 반환타입 없다 , (주소값 반환)
            4. 오버로딩 지원 ( 동일한 이름의 서로 다른 매개변수의 개수/타입/순서 다르면 생성자/메소드명 중복가능 )
        (3) 목적 : 객체생성시 초기화 , 협업간의 규칙/유효성 검사
        (4) 주의할점 : 정의한 클래스내 생성자가 1개도 없으면 기본생성자 (자동) 제공
        (5) this 키워드 : 멤버변수와 매개변수 명이 동일한 경우 멤버변수 가리킴
        * 클래스 내부에서 오른쪽 클릭 -> 소스작업 -> constructor -> 맴버변수 체크 -> [확인] *

